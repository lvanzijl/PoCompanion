@page "/workspace/planning"
@inject NavigationManager NavigationManager

@* Legacy Planning Workspace — redirects to the new Planning hub. *@

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo(WorkspaceRoutes.PlanningWorkspace2, replace: true);
    }
}
