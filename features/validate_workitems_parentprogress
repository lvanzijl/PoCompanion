this feature should validate for each work item if, when it is in progress it's parent should also be in progress.

for example, when a pbi is in progress and the feature is not the feature state is invalid. 

how I would like to see this on the line of the invalid item at the right side of the entry of the invalid item. the PBI/feature is just an example, the same goes for goal and objective or epic and feature etc.

I would also like a filter option 'show only invalid items' which would collapse all items so that the invalid items are visible. chains that are all valid could just be hidden.

for now this is the only validation rule but many will follow.

For now the possible states of all items are the same but soon we will use the real states of the work items and they might all be different.

nothing changes in the on demand loading and caching of work items for now but in the near future we will add functionality which will be able to fix items