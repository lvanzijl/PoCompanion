namespace PoTool.Shared.Settings;

/// <summary>
/// Declares the configured estimation mode for a product.
/// </summary>
public enum EstimationMode
{
    StoryPoints = 0,
    EffortHours = 1,
    Mixed = 2,
    NoSpMode = 3
}
